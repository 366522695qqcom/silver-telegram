apiVersion: v1
kind: Service
metadata:
  name: ai-gateway-backend
  namespace: ai-gateway
spec:
  selector:
    app: ai-gateway-backend
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: ClusterIP
